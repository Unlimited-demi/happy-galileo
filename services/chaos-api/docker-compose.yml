services:
  chaos-api:
    build: .
    container_name: chaos-api
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production
    networks:
      - dev-net

networks:
  dev-net:
    external: true
